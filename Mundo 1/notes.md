### import + nome do módulo
importa um módulo.

* Exemplo: import math 

### import + nome do módulo + as + nome 
importa um módulo e atribui um novo nome ao mesmo.

* Exemplo: import math as matematica

### from + nome do módulo + import + funcionalida
importa uma funcionalidade de um determinado módulo.

* Exemplo: from math import sqrt